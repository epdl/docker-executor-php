version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:stable
      - name: remotedocker
        image: docker:20-dind
        environment:
          DOCKER_TLS_CERTDIR: ''
    steps:
      - checkout
      - run:
          name: Build Docker Image
          command: |
            sh build.sh
workflow:
  build-executor:
    jobs:
      - build
